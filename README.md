# Instagram - Profiles that don't follow you

## 1. How to execute

Place `connections` instagram folder in reposiotry's working directory.

Executre from working directory `go run ./cmd/nonfollowers`